/*#include "std_lib_facilities.h"
#include "salesassoc.h"


int main()
{
    SalesAssoc *ptest = new SalesAssoc("Meghan Tennant" , 668229);
    int token = 0;
    if(ptest->get_name() != "Meghan Tennant")
    {
        token++;
    }
    if(ptest->get_id() != 668229)
    {
        token++;
    }
    if(token != 0)
    {
        cout << ptest->get_name() << " should be Meghan Tennant" << endl;
        cout << ptest->get_id() << " should be 668229" << endl;
    }
    else
    {
        cout << "Passed!" <<endl;
    }
}

*/
